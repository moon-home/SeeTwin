# SeeTwin — Python dependencies
# Install with: pip install -r requirements.txt

# ── Core UI ──────────────────────────────────────────────────────────────────
# Pin the full gradio stack to known-working versions on Python 3.11 / macOS.
# - starlette must be <1.0: gradio 4.44.1 uses the old TemplateResponse(name, context)
#   signature that was removed in starlette 1.0.
# - gradio-client 1.3.0 has a bug in _json_schema_to_python_type that doesn't
#   guard against boolean schemas; patched in-place in utils.py (isinstance check).
gradio==4.44.1
gradio-client==1.3.0
huggingface-hub==0.25.2
starlette<1.0          # gradio 4.44.1 incompatible with starlette 1.x API changes
fastapi==0.136.3       # requires starlette>=0.46.0 (0.52.1 satisfies this)

# ── Background removal (Stage 1) ─────────────────────────────────────────────
transformers>=4.35.0
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0          # required by briaai/RMBG-2.0 model
kornia>=0.7.0        # required by briaai/RMBG-2.0 model
Pillow>=10.0.0
numpy>=1.24.0

# ── Utilities ────────────────────────────────────────────────────────────────
requests>=2.31.0       # model download helper

# ── Stage 3 — Classification ─────────────────────────────────────────────────
open-clip-torch>=3.3.0  # CLIP zero-shot classifier (ViT-B-32, ~350 MB download on first run)
ftfy>=6.0.0             # text normalisation required by open_clip tokenizer

# ── Stages 4–6 (added as stages are built) ───────────────────────────────────
# smplx                # pip install smplx   (after registering at mpi-inf.mpg.de)
# segment-anything-2   # pip install sam2    (Meta SAM2)
# diffusers            # pip install diffusers accelerate  (SD inpainting)
# opencv-python        # pip install opencv-python         (image utils)
